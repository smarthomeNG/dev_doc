.. |_| unicode:: 0xA0


=================================
Plugin 'modbus_tcp' Konfiguration
=================================


.. index:: Plugins; modbus_tcp Konfiguration

.. image:: /plugins/modbus_tcp/webif/static/img/plugin_logo.png
   :alt: plugin logo
   :width: 300px
   :height: 300px
   :scale: 50%
   :align: left

.. |br| raw:: html

   <br />

Im folgenden sind etwaige Anforderungen und unterstützte Hardware beschrieben. Danach folgt die               Beschreibung, wie das Plugin **modbus_tcp** konfiguriert wird. Außerdem ist im folgenden               beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann. [#f1]_ 

Es handelt sich bei diesem Plugin um ein **gateway Plugin**.



Beschreibung
============

Plugin zur Geräte-Anbindung über ModBus an SmartHomeNG


Anforderungen
=============




 - Minimum SmartHomeNG Version: **1.10**
 - Minimum Python Version: **3.8**

Konfiguration
=============


Im folgenden ist beschrieben, wie das Plugin **modbus_tcp** konfiguriert wird. Außerdem ist im folgenden beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann.


=========
Parameter
=========


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/plugin.yaml`` konfiguriert werden:


crontab
=======


Update mit Festlegung via Crontab

 - Datentyp: **str**


cycle
=====


Update Zyklus in Sekunden. Wenn der Wert 0 ist, wird keine Abfrage über cycle ausgeführt

 - Datentyp: **int**
 - Standardwert: **300**

 - Minimalwert: **0**

host
====


IP Adresse des Modbus-Geraetes

 - Datentyp: **ipv4**


pause_item
==========


Item, um die Ausführung des Plugins zu steuern

 - Datentyp: **str**


port
====


modbus Port

 - Datentyp: **int**

 - Minimalwert: **0**
 - Maximalwert: **65535**

slaveUnit
=========


Slave-Addresse der zu lesenden Modbus-Einheit

 - Datentyp: **int**
 - Standardwert: **1**


==============
Item Attribute
==============


Das Plugin unterstützt folgende Item Attribute, die in den Dateien im Verzeichnis  ``../items`` verwendet werden:


modBusAddress
=============


Register Adresse welche gelesen werden soll

 - Datentyp: **num**


modBusByteOrder
===============


Endian.BIG oder Endian.LITTLE

 - Datentyp: **str**
 - Standardwert: **Endian.BIG**

 - Mögliche Werte:

   - **Endian.BIG**
   - **Endian.LITTLE**


modBusDataType
==============


Datentyp vom zu lesenden Register (bit, int16 uint16 int32 uint32 float32 string16 stringNN)

 - Datentyp: **str**
 - Standardwert: **uint16**


modBusDirection
===============


Datenrichtung

 - Datentyp: **str**
 - Standardwert: **read**

 - Mögliche Werte:

   - **read**
   - **read_write**
   - **write**


modBusFactor
============


Faktor mit dem der gelesene Register-Wert multipliziert wird

 - Datentyp: **num**
 - Standardwert: **1**


modBusObjectType
================


Auswahl welcher Objekt-Type gelesen werden soll

 - Datentyp: **str**
 - Standardwert: **HoldingRegister**

 - Mögliche Werte:

   - **Coil**
   - **DiscreteInput**
   - **InputRegister**
   - **HoldingRegister**


modBusUnit
==========


Slave-Addresse der zu lesenden Modbus-Einheit (Unit aus der plugin-Konfig wird überschrieben)

 - Datentyp: **num**
 - Standardwert: **1**


modBusWordOrder
===============


Endian.BIG oder Endian.LITTLE

 - Datentyp: **str**
 - Standardwert: **Endian.BIG**

 - Mögliche Werte:

   - **Endian.BIG**
   - **Endian.LITTLE**


============
Item-Structs
============


Das Plugin stellt die folgenden Item-Structs zur Verfügung. Diese Informationen sind aus der `plugin.yaml` entnommen und möglicherweise nicht vollständig.


huawei
======


.. treeview::
     - :dir:`folder` **huawei** (`foo`, ---)
         - :dir:`folder` **smartlogger** (`foo`, ---)
             - :dir:`folder` **system** (`foo`, ---)
                 - :dir:`file` **date_time** (`num`, ---)
                 - :dir:`file` **time_zone** (`num`, ---)
                 - :dir:`file` **local_time** (`num`, ---)
             - :dir:`folder` **pv** (`foo`, ---)
                 - :dir:`file` **active_power** (`num`, ---)
                 - :dir:`file` **reactive_power** (`num`, ---)
             - :dir:`folder` **storage** (`foo`, ---)
                 - :dir:`file` **active_power** (`num`, ---)
                 - :dir:`file` **soc** (`num`, ---)
             - :dir:`folder` **inverter** (`foo`, ---)
                 - :dir:`file` **efficiency** (`num`, ---)
             - :dir:`folder` **external_meter** (`foo`, ---)
                 - :dir:`folder` **voltage** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **current** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **power** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **power_factor** (`num`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`file` **total_active** (`num`, ---)
                     - :dir:`file` **total_reactive** (`num`, ---)
         - :dir:`folder` **sdongle** (`foo`, ---)
             - :dir:`folder` **energy** (`foo`, ---)
                 - :dir:`file` **input_power** (`num`, ---)
                 - :dir:`file` **load_power** (`num`, ---)
                 - :dir:`file` **grid_power** (`num`, ---)
                 - :dir:`file` **battery_power** (`num`, ---)
                 - :dir:`file` **active_power** (`num`, ---)
         - :dir:`folder` **meter** (`OrderedDict([('meter', OrderedDict([('type', 'num'), ('modBusAddress@instance', 37125), ('modBusDataType@instance', 'uint16'), ('modBusFactor@instance', 1), ('modBusDirection@instance', 'read')])), ('check', OrderedDict([('type', 'num'), ('modBusAddress@instance', 37138), ('modBusDataType@instance', 'uint16'), ('modBusFactor@instance', 1), ('modBusDirection@instance', 'read')]))])`, ---)
             - :dir:`file` **status** (`num`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`folder` **voltage** (`foo`, ---)
                 - :dir:`file` **phase_a** (`num`, ---)
                 - :dir:`file` **phase_b** (`num`, ---)
                 - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`file` **line_ab** (`num`, ---)
                 - :dir:`file` **line_bc** (`num`, ---)
                 - :dir:`file` **line_ca** (`num`, ---)
             - :dir:`folder` **current** (`foo`, ---)
                 - :dir:`file` **phase_a** (`num`, ---)
                 - :dir:`file` **phase_b** (`num`, ---)
                 - :dir:`file` **phase_c** (`num`, ---)
             - :dir:`folder` **power** (`foo`, ---)
                 - :dir:`file` **active** (`num`, ---)
                 - :dir:`file` **reactive** (`num`, ---)
                 - :dir:`file` **factor** (`num`, ---)
                 - :dir:`file` **phase_a** (`num`, ---)
                 - :dir:`file` **phase_b** (`num`, ---)
                 - :dir:`file` **phase_c** (`num`, ---)
             - :dir:`folder` **energy** (`foo`, ---)
                 - :dir:`file` **exported** (`num`, ---)
                 - :dir:`file` **accumulated** (`num`, ---)
                 - :dir:`file` **reactive_accumulated** (`num`, ---)
             - :dir:`folder` **type** (`foo`, ---)
                 - :dir:`file` **meter** (`num`, ---)
                 - :dir:`file` **check** (`num`, ---)
         - :dir:`folder` **inverter** (`foo`, ---)
             - :dir:`folder` **hardware** (`foo`, ---)
                 - :dir:`file` **model_name** (`str`, ---)
                 - :dir:`file` **serial_number** (`str`, ---)
                 - :dir:`file` **part_number** (`str`, ---)
                 - :dir:`file` **model_id** (`num`, ---)
                 - :dir:`file` **product_sales_area** (`str`, ---)
                 - :dir:`file` **grid_standard_code_protocol_version** (`num`, ---)
                 - :dir:`file` **status** (`num`, ---)
                 - :dir:`folder` **capability** (`foo`, ---)
                     - :dir:`file` **nb_pv_strings** (`num`, ---)
                     - :dir:`file` **nb_mpp_tracks** (`num`, ---)
                 - :dir:`file` **startup** (`num`, ---)
                 - :dir:`file` **shutdown** (`num`, ---)
             - :dir:`folder` **software** (`foo`, ---)
                 - :dir:`file` **firmware_version** (`str`, ---)
                 - :dir:`file` **software_version** (`str`, ---)
                 - :dir:`file` **protocol_version_modbus** (`num`, ---)
             - :dir:`folder` **limits** (`foo`, ---)
                 - :dir:`file` **rated_power** (`num`, ---)
                 - :dir:`file` **p_max** (`num`, ---)
                 - :dir:`file` **s_max** (`num`, ---)
                 - :dir:`file` **q_max_out** (`num`, ---)
                 - :dir:`file` **q_max_in** (`num`, ---)
                 - :dir:`file` **p_max_real** (`num`, ---)
                 - :dir:`file` **s_max_real** (`num`, ---)
             - :dir:`folder` **features** (`foo`, ---)
                 - :dir:`file` **feature_mask_1** (`num`, ---)
                 - :dir:`file` **feature_mask_2** (`num`, ---)
                 - :dir:`file` **feature_mask_3** (`num`, ---)
                 - :dir:`file` **feature_mask_4** (`num`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **efficiency** (`num`, ---)
             - :dir:`file` **temperature** (`num`, ---)
             - :dir:`folder` **power** (`foo`, ---)
                 - :dir:`file` **input** (`num`, ---)
                 - :dir:`file` **active** (`num`, ---)
                 - :dir:`file` **reactive** (`num`, ---)
                 - :dir:`file` **factor** (`num`, ---)
         - :dir:`folder` **storage** (`foo`, ---)
             - :dir:`folder` **power** (`foo`, ---)
                 - :dir:`folder` **charge** (`foo`, ---)
                     - :dir:`file` **max** (`num`, ---)
                     - :dir:`file` **max_set** (`num`, ---)
                     - :dir:`folder` **forced** (`foo`, ---)
                         - :dir:`file` **charge** (`num`, ---)
                         - :dir:`file` **discharge** (`num`, ---)
                 - :dir:`folder` **discharge** (`foo`, ---)
                     - :dir:`file` **max** (`num`, ---)
                     - :dir:`file` **max_set** (`num`, ---)
                 - :dir:`folder` **grid_charge** (`foo`, ---)
                     - :dir:`file` **current** (`num`, ---)
                     - :dir:`file` **max** (`num`, ---)
             - :dir:`folder` **capacity** (`foo`, ---)
                 - :dir:`file` **rated** (`num`, ---)
                 - :dir:`file` **state_of_charge** (`num`, ---)
                 - :dir:`folder` **cutoff** (`foo`, ---)
                     - :dir:`file` **charge** (`num`, ---)
                     - :dir:`file` **discharge** (`num`, ---)
                 - :dir:`file` **forced_soc** (`num`, ---)
                 - :dir:`file` **backup_soc** (`num`, ---)
                 - :dir:`file` **grid_charge_cutoff** (`num`, ---)
             - :dir:`folder` **energy** (`foo`, ---)
                 - :dir:`folder` **total** (`foo`, ---)
                     - :dir:`file` **charge** (`num`, ---)
                     - :dir:`file` **discharge** (`num`, ---)
                 - :dir:`folder` **daily** (`foo`, ---)
                     - :dir:`file` **charge** (`num`, ---)
                     - :dir:`file` **discharge** (`num`, ---)
             - :dir:`folder` **electrical** (`foo`, ---)
                 - :dir:`folder` **bus** (`foo`, ---)
                     - :dir:`file` **voltage** (`num`, ---)
                     - :dir:`file` **current** (`num`, ---)
             - :dir:`folder` **status** (`foo`, ---)
                 - :dir:`file` **running** (`num`, ---)
                 - :dir:`file` **forced_mode** (`num`, ---)
                 - :dir:`file` **power_limit_grid** (`num`, ---)
                 - :dir:`file` **working_mode_a** (`num`, ---)
                 - :dir:`file` **working_mode_settings** (`num`, ---)
                 - :dir:`file` **control_mode** (`num`, ---)
             - :dir:`folder` **operation** (`foo`, ---)
                 - :dir:`file` **charge_from_grid** (`bool`, ---)
                 - :dir:`file` **excess_pv_use_tou** (`num`, ---)
             - :dir:`folder` **pricing** (`foo`, ---)
                 - :dir:`file` **time_of_use_price** (`num`, ---)
                 - :dir:`file` **luna_price_periods** (`num`, ---)
                 - :dir:`file` **lg_resu_price_periods** (`num`, ---)
                 - :dir:`file` **fixed_charging_and_discharging_periods** (`num`, ---)
                 - :dir:`file` **lcoe** (`num`, ---)
         - :dir:`folder` **emma** (`foo`, ---)
             - :dir:`folder` **general** (`foo`, ---)
                 - :dir:`folder` **system** (`foo`, ---)
                     - :dir:`file` **software_version** (`str`, ---)
                     - :dir:`file` **model** (`str`, ---)
                     - :dir:`file` **number_of_inverters_found** (`num`, ---)
                     - :dir:`file` **number_of_chargers_found** (`num`, ---)
                     - :dir:`file` **backup_time_notification_threshold** (`num`, ---)
                     - :dir:`file` **local_time_year** (`num`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`folder` **total** (`foo`, ---)
                         - :dir:`file` **absorbed** (`num`, ---)
                         - :dir:`file` **consumption** (`num`, ---)
                         - :dir:`file` **grid_export** (`num`, ---)
                         - :dir:`file` **grid_import** (`num`, ---)
                         - :dir:`file` **inverter_yield** (`num`, ---)
                         - :dir:`file` **pv_yield** (`num`, ---)
                     - :dir:`folder` **today** (`foo`, ---)
                         - :dir:`file` **consumption** (`num`, ---)
                         - :dir:`file` **grid_export** (`num`, ---)
                         - :dir:`file` **grid_import** (`num`, ---)
                         - :dir:`file` **inverter_yield** (`num`, ---)
                         - :dir:`file` **pv_yield** (`num`, ---)
                     - :dir:`folder` **monthly** (`foo`, ---)
                         - :dir:`file` **pv_yield** (`num`, ---)
                         - :dir:`file` **consumption** (`num`, ---)
                         - :dir:`file` **grid_export** (`num`, ---)
                         - :dir:`file` **grid_import** (`num`, ---)
                     - :dir:`folder` **yearly** (`foo`, ---)
                         - :dir:`file` **pv_yield** (`num`, ---)
                         - :dir:`file` **consumption** (`num`, ---)
                         - :dir:`file` **grid_export** (`num`, ---)
                         - :dir:`file` **grid_import** (`num`, ---)
                 - :dir:`folder` **power** (`foo`, ---)
                     - :dir:`file` **pv_output** (`num`, ---)
                     - :dir:`file` **load** (`num`, ---)
                     - :dir:`file` **feed_in** (`num`, ---)
                     - :dir:`folder` **inverter** (`foo`, ---)
                         - :dir:`file` **active** (`num`, ---)
                         - :dir:`file` **rated** (`num`, ---)
                 - :dir:`folder` **configuration** (`foo`, ---)
                     - :dir:`folder` **threephase_imbalance_control** (`bool`, ---)
                         - :dir:`file` **register** (`num`, ---)
                     - :dir:`folder` **power_control_mode_at_grid_connection_point** (`str`, ---)
                         - :dir:`file` **config** (`dict`, ---)
                         - :dir:`file` **config_reverse** (`dict`, ---)
                         - :dir:`file` **register** (`num`, ---)
                     - :dir:`folder` **power_supply_configuration** (`str`, ---)
                         - :dir:`file` **config** (`dict`, ---)
                         - :dir:`file` **config_reverse** (`dict`, ---)
                         - :dir:`file` **register** (`num`, ---)
                     - :dir:`file` **tou_preferred_surplus_pv_use** (`num`, ---)
                     - :dir:`file` **tou_max_grid_charge_power** (`num`, ---)
                     - :dir:`file` **limitation_mode** (`num`, ---)
                     - :dir:`file` **max_feed_in_power_watt** (`num`, ---)
                     - :dir:`file` **max_feed_in_power_percent** (`num`, ---)
             - :dir:`folder` **storage** (`foo`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`file` **chargeable** (`num`, ---)
                     - :dir:`file` **dischargeable** (`num`, ---)
                     - :dir:`folder` **capacity** (`foo`, ---)
                         - :dir:`file` **chargeable** (`num`, ---)
                         - :dir:`file` **dischargeable** (`num`, ---)
                         - :dir:`file` **rated** (`num`, ---)
                     - :dir:`folder` **charged** (`foo`, ---)
                         - :dir:`file` **today** (`num`, ---)
                         - :dir:`file` **monthly** (`num`, ---)
                         - :dir:`file` **total** (`num`, ---)
                     - :dir:`folder` **discharged** (`foo`, ---)
                         - :dir:`file` **today** (`num`, ---)
                         - :dir:`file` **monthly** (`num`, ---)
                         - :dir:`file` **total** (`num`, ---)
                 - :dir:`file` **charge_discharge_power** (`num`, ---)
                 - :dir:`folder` **state_of_charge** (`foo`, ---)
                     - :dir:`file` **primary** (`num`, ---)
                     - :dir:`file` **backup** (`num`, ---)
                 - :dir:`folder` **ess_control_mode** (`str`, ---)
                     - :dir:`file` **config** (`dict`, ---)
                     - :dir:`file` **config_reverse** (`dict`, ---)
                     - :dir:`file` **register** (`num`, ---)
             - :dir:`folder` **built_in_energy_sensor** (`foo`, ---)
                 - :dir:`folder` **voltage** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                     - :dir:`file` **line_ab** (`num`, ---)
                     - :dir:`file` **line_bc** (`num`, ---)
                     - :dir:`file` **line_ca** (`num`, ---)
                 - :dir:`folder` **current** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **power** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **factor** (`num`, ---)
                     - :dir:`file` **apparent** (`num`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **negative** (`num`, ---)
                     - :dir:`file` **positive** (`num`, ---)
             - :dir:`folder` **external_energy_sensor** (`foo`, ---)
                 - :dir:`folder` **voltage** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                     - :dir:`file` **line_ab** (`num`, ---)
                     - :dir:`file` **line_bc** (`num`, ---)
                     - :dir:`file` **line_ca** (`num`, ---)
                 - :dir:`folder` **current** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **power** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **factor** (`num`, ---)
                     - :dir:`file` **apparent** (`num`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **negative** (`num`, ---)
                     - :dir:`file` **positive** (`num`, ---)
             - :dir:`folder` **external_meter** (`foo`, ---)
                 - :dir:`file` **status** (`num`, ---)
                 - :dir:`folder` **voltage** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                     - :dir:`file` **line_ab** (`num`, ---)
                     - :dir:`file` **line_bc** (`num`, ---)
                     - :dir:`file` **line_ca** (`num`, ---)
                 - :dir:`folder` **current** (`foo`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **power** (`foo`, ---)
                     - :dir:`file` **active** (`num`, ---)
                     - :dir:`file` **apparent** (`num`, ---)
                     - :dir:`file` **factor** (`num`, ---)
                     - :dir:`file` **phase_a** (`num`, ---)
                     - :dir:`file` **phase_b** (`num`, ---)
                     - :dir:`file` **phase_c** (`num`, ---)
                 - :dir:`folder` **energy** (`foo`, ---)
                     - :dir:`file` **total** (`num`, ---)
                     - :dir:`file` **positive** (`num`, ---)
                     - :dir:`file` **negative** (`num`, ---)


===============
Logik Parameter
===============


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/logic.yaml`` konfiguriert werden:

Keine Logik Parameter in den Metadaten beschrieben - **Bitte in der README nachsehen** (siehe Fußnote)

================
Plugin Functions
================


Das Plugin verfügt über folgende öffentliche Funktionen, die z.B. in Logiken aufgerufen werden können.

**Keine**

.. [#f1] Diese Seite wurde aus den Metadaten des Plugins erzeugt.
