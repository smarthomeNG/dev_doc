.. |_| unicode:: 0xA0


============================
Plugin 'denon' Konfiguration
============================


.. index:: Plugins; denon Konfiguration

.. image:: /plugins/denon/webif/static/img/plugin_logo.svg
   :alt: plugin logo
   :width: 300px
   :height: 300px
   :scale: 50%
   :align: left

.. |br| raw:: html

   <br />

Im folgenden sind etwaige Anforderungen und unterstützte Hardware beschrieben. Danach folgt die               Beschreibung, wie das Plugin **denon** konfiguriert wird. Außerdem ist im folgenden               beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann. [#f1]_ 

Es handelt sich bei diesem Plugin um ein **interface Plugin**.

**ACHTUNG**: Dieses Plugin ist als develop gekennzeichnet. Es kann daher sein, dass es                  noch nicht sämtliche Funktionen unterstützt oder noch fehlerhaft ist.



Beschreibung
============




Anforderungen
=============




 - Minimum SmartHomeNG Version: **1.9.5**
 - Minimum Python Version: **3.7**

Konfiguration
=============


Im folgenden ist beschrieben, wie das Plugin **denon** konfiguriert wird. Außerdem ist im folgenden beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann.


=========
Parameter
=========


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/plugin.yaml`` konfiguriert werden:


autoconnect
===========




 - Datentyp: **bool**


autoreconnect
=============




 - Datentyp: **bool**
 - Standardwert: **True**


baudrate
========




 - Datentyp: **num**
 - Standardwert: **9600**


binary
======




 - Datentyp: **bool**
 - Standardwert: **False**


bytesize
========




 - Datentyp: **num**
 - Standardwert: **8**


conn_type
=========




 - Datentyp: **str**

 - Mögliche Werte:

   - 
   - **net_tcp_client**
   - **serial_async**


connect_cycle
=============




 - Datentyp: **num**
 - Standardwert: **3**


connect_retries
===============




 - Datentyp: **num**
 - Standardwert: **5**


delay_initial_read
==================




 - Datentyp: **num**
 - Standardwert: **0**


host
====




 - Datentyp: **str**


model
=====




 - Datentyp: **str**

 - Mögliche Werte:

   - 
   - **AVR-X6300H**
   - **AVR-X6300H-NA**
   - **AVR-X4300H**
   - **AVR-X3300W**
   - **AVR-X2300W**
   - **AVR-X1300W**


parity
======




 - Datentyp: **str**
 - Standardwert: **N**

 - Mögliche Werte:

   - **N**
   - **E**
   - **O**
   - **M**
   - **S**


port
====




 - Datentyp: **int**
 - Standardwert: **23**


resume_initial_read
===================




 - Datentyp: **bool**


retry_cycle
===========




 - Datentyp: **num**
 - Standardwert: **30**


retry_suspend
=============




 - Datentyp: **num**
 - Standardwert: **3**


send_retries
============




 - Datentyp: **num**
 - Standardwert: **0**


send_retries_cycle
==================




 - Datentyp: **num**
 - Standardwert: **1**

 - Minimalwert: **1**

serialport
==========




 - Datentyp: **str**


stopbits
========




 - Datentyp: **num**
 - Standardwert: **1**


suspend_item
============




 - Datentyp: **str**


terminator
==========




 - Datentyp: **str**
 - Standardwert: **\r**


timeout
=======




 - Datentyp: **num**
 - Standardwert: **3**


==============
Item Attribute
==============


Das Plugin unterstützt folgende Item Attribute, die in den Dateien im Verzeichnis  ``../items`` verwendet werden:


denon_command
=============




 - Datentyp: **str**


denon_lookup
============




 - Datentyp: **str**


denon_read
==========




 - Datentyp: **bool**


denon_read_cycle
================




 - Datentyp: **num**


denon_read_group
================




 - Datentyp: **list(str)**


denon_read_group_trigger
========================




 - Datentyp: **str**


denon_read_initial
==================




 - Datentyp: **bool**


denon_readafterwrite
====================




 - Datentyp: **num**


denon_write
===========




 - Datentyp: **bool**


============
Item-Structs
============


Das Plugin stellt die folgenden Item-Structs zur Verfügung. Diese Informationen sind aus der `plugin.yaml` entnommen und möglicherweise nicht vollständig.


ALL
===


.. treeview::
     - :dir:`folder` **ALL** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)


AVR-X1300W
==========


.. treeview::
     - :dir:`folder` **AVR-X1300W** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **album** (`str`, ---)
             - :dir:`file` **artist** (`str`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **stationname** (`str`, ---)
             - :dir:`file` **frequencyup** (`bool`, ---)
             - :dir:`file` **frequencydown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)
             - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **dialogtoggle** (`bool`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)


AVR-X2300W
==========


.. treeview::
     - :dir:`folder` **AVR-X2300W** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **album** (`str`, ---)
             - :dir:`file` **artist** (`str`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **stationname** (`str`, ---)
             - :dir:`file` **frequencyup** (`bool`, ---)
             - :dir:`file` **frequencydown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)
             - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **dialogtoggle** (`bool`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
                 - :dir:`folder` **video** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **aspectratio** (`str`, ---)
                     - :dir:`file` **hdmimonitor** (`num`, ---)
                     - :dir:`folder` **hdmiresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoprocessingmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **pictureenhancer** (`num`, ---)
                     - :dir:`folder` **videoinput** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)


AVR-X3300W
==========


.. treeview::
     - :dir:`folder` **AVR-X3300W** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **album** (`str`, ---)
             - :dir:`file` **artist** (`str`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **stationname** (`str`, ---)
             - :dir:`file` **frequencyup** (`bool`, ---)
             - :dir:`file` **frequencydown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)
             - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwoofer2** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **dialogtoggle** (`bool`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
                 - :dir:`folder` **video** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **aspectratio** (`str`, ---)
                     - :dir:`folder` **hdmiresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoprocessingmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **pictureenhancer** (`num`, ---)
                     - :dir:`folder` **videoinput** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)


AVR-X4300H
==========


.. treeview::
     - :dir:`folder` **AVR-X4300H** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **stationname** (`str`, ---)
             - :dir:`file` **frequencyup** (`bool`, ---)
             - :dir:`file` **frequencydown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)
             - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwoofer2** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **speakersetup** (`str`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **dialogtoggle** (`bool`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
                 - :dir:`folder` **video** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **aspectratio** (`str`, ---)
                     - :dir:`file` **hdmimonitor** (`num`, ---)
                     - :dir:`folder` **hdmiresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoprocessingmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **pictureenhancer** (`num`, ---)
                     - :dir:`folder` **videoinput** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)


AVR-X6300H
==========


.. treeview::
     - :dir:`folder` **AVR-X6300H** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **info** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **fullmodel** (`str`, ---)
             - :dir:`file` **model** (`str`, ---)
             - :dir:`file` **serialnumber** (`num`, ---)
             - :dir:`file` **main** (`str`, ---)
             - :dir:`file` **mainfbl** (`num`, ---)
             - :dir:`file` **dsp1** (`num`, ---)
             - :dir:`file` **dsp2** (`num`, ---)
             - :dir:`file` **dsp3** (`num`, ---)
             - :dir:`file` **dsp4** (`num`, ---)
             - :dir:`file` **apld** (`num`, ---)
             - :dir:`file` **vpld** (`num`, ---)
             - :dir:`file` **guidat** (`num`, ---)
             - :dir:`file` **heosversion** (`str`, ---)
             - :dir:`file` **heosbuild** (`num`, ---)
             - :dir:`file` **heosmod** (`num`, ---)
             - :dir:`file` **heoscnf** (`str`, ---)
             - :dir:`file` **heoslanguage** (`str`, ---)
             - :dir:`file` **mac** (`str`, ---)
             - :dir:`file` **wifimac** (`str`, ---)
             - :dir:`file` **btmac** (`str`, ---)
             - :dir:`file` **audyif** (`num`, ---)
             - :dir:`file` **productid** (`num`, ---)
             - :dir:`file` **packageid** (`num`, ---)
             - :dir:`file` **cmp** (`str`, ---)
             - :dir:`file` **region** (`str`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **frequency** (`num`, ---)
             - :dir:`file` **stationname** (`str`, ---)
             - :dir:`file` **frequencyup** (`bool`, ---)
             - :dir:`file` **frequencydown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)
             - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwoofer2** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **speakersetup** (`str`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **dialogenhance** (`num`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **digitalinput** (`str`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
                 - :dir:`folder` **video** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **aspectratio** (`str`, ---)
                     - :dir:`file` **hdmimonitor** (`num`, ---)
                     - :dir:`folder` **hdmiresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoprocessingmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **pictureenhancer** (`num`, ---)
                     - :dir:`folder` **videoinput** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)


AVR-X6300H-NA
=============


.. treeview::
     - :dir:`folder` **AVR-X6300H-NA** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **info** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **fullmodel** (`str`, ---)
             - :dir:`file` **model** (`str`, ---)
             - :dir:`file` **serialnumber** (`num`, ---)
             - :dir:`file` **main** (`str`, ---)
             - :dir:`file` **mainfbl** (`num`, ---)
             - :dir:`file` **dsp1** (`num`, ---)
             - :dir:`file` **dsp2** (`num`, ---)
             - :dir:`file` **dsp3** (`num`, ---)
             - :dir:`file` **dsp4** (`num`, ---)
             - :dir:`file` **apld** (`num`, ---)
             - :dir:`file` **vpld** (`num`, ---)
             - :dir:`file` **guidat** (`num`, ---)
             - :dir:`file` **heosversion** (`str`, ---)
             - :dir:`file` **heosbuild** (`num`, ---)
             - :dir:`file` **heosmod** (`num`, ---)
             - :dir:`file` **heoscnf** (`str`, ---)
             - :dir:`file` **heoslanguage** (`str`, ---)
             - :dir:`file` **mac** (`str`, ---)
             - :dir:`file` **wifimac** (`str`, ---)
             - :dir:`file` **btmac** (`str`, ---)
             - :dir:`file` **audyif** (`num`, ---)
             - :dir:`file` **productid** (`num`, ---)
             - :dir:`file` **packageid** (`num`, ---)
             - :dir:`file` **cmp** (`str`, ---)
             - :dir:`file` **region** (`str`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **custom_inputnames** (`dict`, ---)
                 - :dir:`file` **finished** (`bool`, ---)
                 - :dir:`file` **input3** (`dict`, ---)
                 - :dir:`file` **update** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **setupmenu** (`bool`, ---)
             - :dir:`folder` **soundmode** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputsignal** (`str`, ---)
             - :dir:`folder` **inputrate** (`num`, ---)
                 - :dir:`folder` **poll** (`bool`, ---)
                     - :dir:`file` **interval** (`num`, ---)
             - :dir:`file` **inputformat** (`str`, ---)
             - :dir:`file` **inputresolution** (`str`, ---)
             - :dir:`file` **outputresolution** (`str`, ---)
             - :dir:`file` **ecomode** (`str`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **hd** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **channel** (`num`, ---)
                 - :dir:`file` **channelup** (`bool`, ---)
                 - :dir:`file` **channeldown** (`bool`, ---)
                 - :dir:`file` **multicastchannel** (`num`, ---)
                 - :dir:`file` **presetmemory** (`num`, ---)
                 - :dir:`file` **preset** (`num`, ---)
                 - :dir:`file` **presetup** (`bool`, ---)
                 - :dir:`file` **presetdown** (`bool`, ---)
                 - :dir:`file` **band** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **volumemax** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **listeningmode** (`str`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **surroundback_left** (`num`, ---)
                         - :dir:`file` **surroundback_right** (`num`, ---)
                         - :dir:`file` **rear_height_left** (`num`, ---)
                         - :dir:`file` **rear_height_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwoofer2** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                         - :dir:`file` **lfe** (`num`, ---)
                         - :dir:`file` **lfeup** (`bool`, ---)
                         - :dir:`file` **lfedown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **cinema_eq** (`bool`, ---)
                         - :dir:`file` **speakersetup** (`str`, ---)
                         - :dir:`file` **hdmiaudioout** (`str`, ---)
                         - :dir:`file` **dynamicrange** (`num`, ---)
                         - :dir:`file` **effect** (`num`, ---)
                         - :dir:`file` **effectup** (`bool`, ---)
                         - :dir:`file` **delay** (`num`, ---)
                         - :dir:`file` **delayup** (`bool`, ---)
                         - :dir:`file` **delaydown** (`bool`, ---)
                         - :dir:`file` **dialogenhance** (`num`, ---)
                         - :dir:`file` **subwoofertoggle** (`bool`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                         - :dir:`file` **subwooferup** (`bool`, ---)
                         - :dir:`file` **subwooferdown** (`bool`, ---)
                         - :dir:`file` **audioinput** (`str`, ---)
                 - :dir:`folder` **video** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **aspectratio** (`str`, ---)
                     - :dir:`file` **hdmimonitor** (`num`, ---)
                     - :dir:`folder` **hdmiresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoprocessingmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **videoresolution** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **pictureenhancer** (`num`, ---)
                     - :dir:`folder` **videoinput** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **hdmiout** (`str`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`file` **sleep** (`num`, ---)
                 - :dir:`file` **standby** (`num`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **HPF** (`bool`, ---)


general
=======


.. treeview::
     - :dir:`folder` **general** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **custom_inputnames** (`dict`, ---)
             - :dir:`file` **finished** (`bool`, ---)
             - :dir:`file` **input3** (`dict`, ---)
             - :dir:`file` **update** (`bool`, ---)
         - :dir:`file` **power** (`bool`, ---)
         - :dir:`file` **setupmenu** (`bool`, ---)
         - :dir:`file` **display** (`str`, ---)
         - :dir:`folder` **soundmode** (`str`, ---)
             - :dir:`file` **lookup** (`list`, ---)
         - :dir:`file` **allzonestereo** (`bool`, ---)
         - :dir:`file` **inputsignal** (`str`, ---)
         - :dir:`folder` **inputrate** (`num`, ---)
             - :dir:`folder` **poll** (`bool`, ---)
                 - :dir:`file` **interval** (`num`, ---)
         - :dir:`file` **inputformat** (`str`, ---)
         - :dir:`file` **inputresolution** (`str`, ---)
         - :dir:`file` **outputresolution** (`str`, ---)
         - :dir:`file` **ecomode** (`str`, ---)


info
====


.. treeview::
     - :dir:`folder` **info** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`file` **fullmodel** (`str`, ---)
         - :dir:`file` **model** (`str`, ---)
         - :dir:`file` **serialnumber** (`num`, ---)
         - :dir:`file` **main** (`str`, ---)
         - :dir:`file` **mainfbl** (`num`, ---)
         - :dir:`file` **dsp1** (`num`, ---)
         - :dir:`file` **dsp2** (`num`, ---)
         - :dir:`file` **dsp3** (`num`, ---)
         - :dir:`file` **dsp4** (`num`, ---)
         - :dir:`file` **apld** (`num`, ---)
         - :dir:`file` **vpld** (`num`, ---)
         - :dir:`file` **guidat** (`num`, ---)
         - :dir:`file` **heosversion** (`str`, ---)
         - :dir:`file` **heosbuild** (`num`, ---)
         - :dir:`file` **heosmod** (`num`, ---)
         - :dir:`file` **heoscnf** (`str`, ---)
         - :dir:`file` **heoslanguage** (`str`, ---)
         - :dir:`file` **mac** (`str`, ---)
         - :dir:`file` **wifimac** (`str`, ---)
         - :dir:`file` **btmac** (`str`, ---)
         - :dir:`file` **audyif** (`num`, ---)
         - :dir:`file` **productid** (`num`, ---)
         - :dir:`file` **packageid** (`num`, ---)
         - :dir:`file` **cmp** (`str`, ---)
         - :dir:`file` **region** (`str`, ---)


tuner
=====


.. treeview::
     - :dir:`folder` **tuner** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`file` **title** (`str`, ---)
         - :dir:`file` **album** (`str`, ---)
         - :dir:`file` **artist** (`str`, ---)
         - :dir:`file` **presetmemory** (`num`, ---)
         - :dir:`file` **preset** (`num`, ---)
         - :dir:`file` **presetup** (`bool`, ---)
         - :dir:`file` **presetdown** (`bool`, ---)
         - :dir:`file` **frequency** (`num`, ---)
         - :dir:`file` **stationname** (`str`, ---)
         - :dir:`file` **frequencyup** (`bool`, ---)
         - :dir:`file` **frequencydown** (`bool`, ---)
         - :dir:`file` **band** (`str`, ---)
         - :dir:`file` **tuningmode** (`str`, ---)
         - :dir:`folder` **hd** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **channel** (`num`, ---)
             - :dir:`file` **channelup** (`bool`, ---)
             - :dir:`file` **channeldown** (`bool`, ---)
             - :dir:`file` **multicastchannel** (`num`, ---)
             - :dir:`file` **presetmemory** (`num`, ---)
             - :dir:`file` **preset** (`num`, ---)
             - :dir:`file` **presetup** (`bool`, ---)
             - :dir:`file` **presetdown** (`bool`, ---)
             - :dir:`file` **band** (`str`, ---)


zone1
=====


.. treeview::
     - :dir:`folder` **zone1** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`file` **volumemax** (`num`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **listeningmode** (`str`, ---)
             - :dir:`file` **sleep** (`num`, ---)
             - :dir:`file` **standby** (`num`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`file` **front_height_left** (`num`, ---)
                     - :dir:`file` **front_height_right** (`num`, ---)
                     - :dir:`file` **front_center** (`num`, ---)
                     - :dir:`file` **surround_left** (`num`, ---)
                     - :dir:`file` **surround_right** (`num`, ---)
                     - :dir:`file` **surroundback_left** (`num`, ---)
                     - :dir:`file` **surroundback_right** (`num`, ---)
                     - :dir:`file` **rear_height_left** (`num`, ---)
                     - :dir:`file` **rear_height_right** (`num`, ---)
                     - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`file` **subwoofer2** (`num`, ---)
                 - :dir:`folder` **tone_control** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **tone** (`bool`, ---)
                     - :dir:`file` **treble** (`num`, ---)
                     - :dir:`file` **trebleup** (`bool`, ---)
                     - :dir:`file` **trebledown** (`bool`, ---)
                     - :dir:`file` **bass** (`num`, ---)
                     - :dir:`file` **bassup** (`bool`, ---)
                     - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`file` **lfe** (`num`, ---)
                     - :dir:`file` **lfeup** (`bool`, ---)
                     - :dir:`file` **lfedown** (`bool`, ---)
                 - :dir:`folder` **general** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **cinema_eq** (`bool`, ---)
                     - :dir:`file` **dynamic_eq** (`bool`, ---)
                     - :dir:`file` **multeq** (`bool`, ---)
                     - :dir:`file` **dynamic_vol** (`bool`, ---)
                     - :dir:`file` **speakersetup** (`str`, ---)
                     - :dir:`file` **hdmiaudioout** (`str`, ---)
                     - :dir:`file` **dynamicrange** (`num`, ---)
                     - :dir:`file` **dialogtoggle** (`bool`, ---)
                     - :dir:`file` **dialog** (`num`, ---)
                     - :dir:`file` **dialogup** (`bool`, ---)
                     - :dir:`file` **dialogdown** (`bool`, ---)
                     - :dir:`file` **effect** (`num`, ---)
                     - :dir:`file` **effectup** (`bool`, ---)
                     - :dir:`file` **effectdown** (`bool`, ---)
                     - :dir:`file` **delay** (`num`, ---)
                     - :dir:`file` **delayup** (`bool`, ---)
                     - :dir:`file` **delaydown** (`bool`, ---)
                     - :dir:`file` **dialogenhance** (`num`, ---)
                     - :dir:`file` **subwoofertoggle** (`bool`, ---)
                     - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`file` **subwooferup** (`bool`, ---)
                     - :dir:`file` **subwooferdown** (`bool`, ---)
                     - :dir:`file` **digitalinput** (`str`, ---)
                     - :dir:`file` **audioinput** (`str`, ---)
             - :dir:`folder` **video** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **aspectratio** (`str`, ---)
                 - :dir:`file` **hdmimonitor** (`num`, ---)
                 - :dir:`folder` **hdmiresolution** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **videoprocessingmode** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **videoresolution** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **pictureenhancer** (`num`, ---)
                 - :dir:`folder` **videoinput** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)


zone2
=====


.. treeview::
     - :dir:`folder` **zone2** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **sleep** (`num`, ---)
             - :dir:`file` **standby** (`num`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)
                 - :dir:`folder` **tone_control** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **treble** (`num`, ---)
                     - :dir:`file` **trebleup** (`bool`, ---)
                     - :dir:`file` **trebledown** (`bool`, ---)
                     - :dir:`file` **bass** (`num`, ---)
                     - :dir:`file` **bassup** (`bool`, ---)
                     - :dir:`file` **bassdown** (`bool`, ---)
                 - :dir:`folder` **general** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdmiout** (`str`, ---)
                     - :dir:`file` **HPF** (`bool`, ---)


zone3
=====


.. treeview::
     - :dir:`folder` **zone3** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`file` **sleep** (`num`, ---)
             - :dir:`file` **standby** (`num`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)
                 - :dir:`folder` **tone_control** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **treble** (`num`, ---)
                     - :dir:`file` **trebleup** (`bool`, ---)
                     - :dir:`file` **trebledown** (`bool`, ---)
                     - :dir:`file` **bass** (`num`, ---)
                     - :dir:`file` **bassup** (`bool`, ---)
                     - :dir:`file` **bassdown** (`bool`, ---)
                 - :dir:`folder` **general** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **HPF** (`bool`, ---)


===============
Logik Parameter
===============


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/logic.yaml`` konfiguriert werden:

Keine Logik Parameter in den Metadaten beschrieben - **Bitte in der README nachsehen** (siehe Fußnote)

================
Plugin Functions
================


Das Plugin verfügt über folgende öffentliche Funktionen, die z.B. in Logiken aufgerufen werden können.

**Keine**

.. [#f1] Diese Seite wurde aus den Metadaten des Plugins erzeugt.
