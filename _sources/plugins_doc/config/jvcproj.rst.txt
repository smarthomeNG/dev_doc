.. |_| unicode:: 0xA0


==============================
Plugin 'jvcproj' Konfiguration
==============================


.. index:: Plugins; jvcproj Konfiguration

.. image:: /plugins/jvcproj/webif/static/img/plugin_logo.png
   :alt: plugin logo
   :width: 300px
   :height: 300px
   :scale: 50%
   :align: left

.. |br| raw:: html

   <br />

Im folgenden sind etwaige Anforderungen und unterstützte Hardware beschrieben. Danach folgt die               Beschreibung, wie das Plugin **jvcproj** konfiguriert wird. Außerdem ist im folgenden               beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann. [#f1]_ 

Es handelt sich bei diesem Plugin um ein **interface Plugin**.



Beschreibung
============




Anforderungen
=============




 - Minimum SmartHomeNG Version: **1.6**

Konfiguration
=============


Im folgenden ist beschrieben, wie das Plugin **jvcproj** konfiguriert wird. Außerdem ist im folgenden beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann.


=========
Parameter
=========


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/plugin.yaml`` konfiguriert werden:


gammaconf_dir
=============




 - Datentyp: **str**
 - Standardwert: **/usr/local/smarthome/etc/jvcproj/**


host
====




 - Datentyp: **ip**
 - Standardwert: **0.0.0.0**


==============
Item Attribute
==============


Das Plugin unterstützt folgende Item Attribute, die in den Dateien im Verzeichnis  ``../items`` verwendet werden:


jvcproj_cmd
===========




 - Datentyp: **str**


jvcproj_gamma
=============




 - Datentyp: **str**


============
Item-Structs
============


Das Plugin stellt die folgenden Item-Structs zur Verfügung. Diese Informationen sind aus der `plugin.yaml` entnommen und möglicherweise nicht vollständig.


jvcproj
=======


.. treeview::
     - :dir:`folder` **jvcproj** (`foo`, ---)
         - :dir:`file` **testcmd** (`bool`, ---)
         - :dir:`file` **pwon** (`bool`, ---)
         - :dir:`file` **pwoff** (`bool`, ---)
         - :dir:`folder` **picmode** (`foo`, ---)
             - :dir:`file` **cmdoff** (`bool`, ---)
             - :dir:`file` **cmdlow** (`bool`, ---)
             - :dir:`file` **sdrlow** (`bool`, ---)
             - :dir:`file` **sdrhigh** (`bool`, ---)
             - :dir:`file` **hdrlow** (`bool`, ---)
             - :dir:`file` **hdrhigh** (`bool`, ---)
         - :dir:`folder` **gamma** (`foo`, ---)
             - :dir:`file` **hdrlow** (`bool`, ---)
             - :dir:`file` **hdrmid** (`bool`, ---)
             - :dir:`file` **hdrhigh** (`bool`, ---)
         - :dir:`folder` **lens** (`foo`, ---)
             - :dir:`file` **mem219** (`bool`, ---)
             - :dir:`file` **mem169** (`bool`, ---)
         - :dir:`folder` **rc** (`foo`, ---)
             - :dir:`file` **menu** (`bool`, ---)
             - :dir:`file` **ok** (`bool`, ---)
             - :dir:`file` **up** (`bool`, ---)
             - :dir:`file` **down** (`bool`, ---)
             - :dir:`file` **left** (`bool`, ---)
             - :dir:`file` **right** (`bool`, ---)
             - :dir:`file` **info** (`bool`, ---)
             - :dir:`file` **back** (`bool`, ---)


===============
Logik Parameter
===============


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/logic.yaml`` konfiguriert werden:

Keine Logik Parameter in den Metadaten beschrieben - **Bitte in der README nachsehen** (siehe Fußnote)

================
Plugin Functions
================


Das Plugin verfügt über folgende öffentliche Funktionen, die z.B. in Logiken aufgerufen werden können.

**Keine**

.. [#f1] Diese Seite wurde aus den Metadaten des Plugins erzeugt.
