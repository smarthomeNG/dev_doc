.. |_| unicode:: 0xA0


==========================
Plugin 'lms' Konfiguration
==========================


.. index:: Plugins; lms Konfiguration

.. image:: /plugins/lms/webif/static/img/plugin_logo.svg
   :alt: plugin logo
   :width: 300px
   :height: 300px
   :scale: 50%
   :align: left

.. |br| raw:: html

   <br />

Im folgenden sind etwaige Anforderungen und unterstützte Hardware beschrieben. Danach folgt die               Beschreibung, wie das Plugin **lms** konfiguriert wird. Außerdem ist im folgenden               beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann. [#f1]_ 

Es handelt sich bei diesem Plugin um ein **interface Plugin**.

**ACHTUNG**: Dieses Plugin ist als develop gekennzeichnet. Es kann daher sein, dass es                  noch nicht sämtliche Funktionen unterstützt oder noch fehlerhaft ist.



Beschreibung
============

Logitech Mediaserver


Anforderungen
=============




 - Minimum SmartHomeNG Version: **1.9.5**
 - Minimum Python Version: **3.7**

Konfiguration
=============


Im folgenden ist beschrieben, wie das Plugin **lms** konfiguriert wird. Außerdem ist im folgenden beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann.


=========
Parameter
=========


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/plugin.yaml`` konfiguriert werden:


autoconnect
===========


Automatisches Verbinden bei Senden

 - Datentyp: **bool**


autoreconnect
=============


Automatisches Neuverbinden bei Abbruch

 - Datentyp: **bool**
 - Standardwert: **True**


connect_cycle
=============


Pause zwischen Verbindungsversuchen

 - Datentyp: **num**
 - Standardwert: **3**


connect_retries
===============


Anzahl Verbindungsversuche

 - Datentyp: **num**
 - Standardwert: **5**


delay_initial_read
==================


Warte nach Verbindungsaufbau mit dem Abfragen von Werten

 - Datentyp: **num**
 - Standardwert: **0**


host
====


Netzwerkziel/-host

 - Datentyp: **str**


port
====


Port für CLI Netzwerkverbindung

 - Datentyp: **int**
 - Standardwert: **9090**


recursive_custom
================


Vererbung von "custom" Itemattributen an untergeordnete Items aktivieren

 - Datentyp: **bool**
 - Standardwert: **True**


resume_initial_read
===================


Bei resume vom Plugin erstmaliges Lesen erneut durchführen

 - Datentyp: **bool**


retry_cycle
===========


Pause zwischen Durchgängen von Verbindungsversuchen

 - Datentyp: **num**
 - Standardwert: **30**


retry_suspend
=============


Anzahl von Durchgängen vor Verbindungsabbruch oder Suspend-Modus

 - Datentyp: **num**
 - Standardwert: **3**


send_retries
============


Anzahl Sendeversuche
 Kommt keine (passende) Antwort nach dem Senden  eines Commands zurück, wird das Kommando nochmals  gesendet, sofern der Wert über 0 liegt. 

 - Datentyp: **num**
 - Standardwert: **0**


send_retries_cycle
==================


Pause zwischen Durchgängen von Sendeversuchen
 Sind Send Retries aktiv, wird ein Scheduler erstellt,  der im angegebenen Sekundentakt Kommandos erneut sendet,  zu denen keine (passenden) Antworten erhalten wurden. 

 - Datentyp: **num**
 - Standardwert: **1**

 - Minimalwert: **1**

suspend_item
============


Item-Pfad für das Standby-Item

 - Datentyp: **str**


terminator
==========


Zeilen-/Antwortbegrenzer

 - Datentyp: **str**
 - Standardwert: **\r**


timeout
=======


Timeout für Geräteantwort

 - Datentyp: **num**
 - Standardwert: **3**


web_host
========


Host für Webinterface-Verbindung (nötig für coverarturl bei Einsatz eines Reverse Proxy). Wird dieser Parameter nicht definiert, wird die URL unter "Host" herangezogen.

 - Datentyp: **str**


web_port
========


Port für Webinterface-Verbindung (nötig für coverarturl). Wird der Port auf 0 gesetzt, wird für die Coverart URL kein Port verwendet (z.B. beim Einsatz eines Reverse Proxy).

 - Datentyp: **int**
 - Standardwert: **9000**


==============
Item Attribute
==============


Das Plugin unterstützt folgende Item Attribute, die in den Dateien im Verzeichnis  ``../items`` verwendet werden:


lms_command
===========


Legt das angegebene Kommando für das Item fest

 - Datentyp: **str**


lms_custom1
===========


MAC-Adresse des Wiedergabegerätes

 - Datentyp: **str**


lms_lookup
==========


Der Inhalt der Lookup-Tabelle mit dem angegebenen Namen wird beim  Start einmalig als dict oder list in das Item geschrieben.    Durch Anhängen von "#<mode>" an den Namen der Tabelle kann die Art  der Tabelle ausgewählt werden:  - fwd liefert die Tabelle Gerät -> SmartHomeNG (Standard)  - rev liefert die Tabelle SmartHomeNG -> Gerät  - rci liefert die Tabelle SmarthomeNG -> Gerät in Kleinbuchstaben  - list liefert die Liste der Namen für SmartHomeNG (z.B. für Auswahllisten in der Visu)

 - Datentyp: **str**


lms_read
========


Item liest/erhält Werte vom Gerät

 - Datentyp: **bool**


lms_read_cycle
==============


Konfiguriert ein Intervall in Sekunden für regelmäßiges Lesen

 - Datentyp: **num**


lms_read_group
==============


Weist das Item der angegebenen Gruppe zum gesammelten Lesen zu. Mehrere Gruppen können als Liste angegeben werden.

 - Datentyp: **list(str)**


lms_read_group_trigger
======================


Wenn diesem Item ein beliebiger Wert zugewiesen wird, werden alle zum Lesen konfigurierten Items der angegebenen Gruppe neu vom Gerät gelesen, bei Gruppe 0 werden alle zum Lesen konfigurierten Items neu gelesen. Das Item kann nicht gleichzeitig mit lms_command belegt werden.

 - Datentyp: **str**


lms_read_initial
================


Legt fest, dass der Wert beim Start vom Gerät gelesen wird

 - Datentyp: **bool**


lms_valid_list
==============


Kommando, das eine valid_list(_re/_ci) definiert hat. Der Inhalt dieses Items ersetzt die Liste des entsprechenden Befehls.

 - Datentyp: **str**


lms_write
=========


Änderung des Items werden an das Gerät gesendet

 - Datentyp: **bool**


============
Item-Structs
============


Das Plugin stellt die folgenden Item-Structs zur Verfügung. Diese Informationen sind aus der `plugin.yaml` entnommen und möglicherweise nicht vollständig.


ALL
===


.. treeview::
     - :dir:`folder` **ALL** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **server** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **version** (`str`, ---)
             - :dir:`file` **restart** (`bool`, ---)
             - :dir:`file` **listenmode** (`bool`, ---)
             - :dir:`file` **playercount** (`num`, ---)
             - :dir:`file` **favoritescount** (`num`, ---)
             - :dir:`folder` **syncgroups** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **members** (`list`, ---)
                 - :dir:`file` **names** (`list`, ---)
             - :dir:`file` **newclient** (`str`, ---)
             - :dir:`file` **forgetclient** (`str`, ---)
             - :dir:`folder` **players** (`dict`, ---)
                 - :dir:`folder` **lu_names** (`dict`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **playlists** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **available** (`dict`, ---)
                     - :dir:`folder` **lu_ids** (`dict`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **lu_urls** (`dict`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **rename** (`str`, "needed value:<playlist_id> <newname> with a space inbetween")
                 - :dir:`folder` **delete** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **database** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **rescan** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **start** (`str`, "playlists|onlinelibrary|external|full|full file://some/path")
                 - :dir:`file` **running** (`bool`, ---)
                 - :dir:`folder` **progress** (`str`, ---)
                     - :dir:`file` **poll** (`bool`, ---)
                     - :dir:`file` **starttime** (`str`, ---)
                     - :dir:`file` **info** (`str`, ---)
                     - :dir:`file` **step** (`num`, ---)
                     - :dir:`file` **totalsteps** (`num`, ---)
                 - :dir:`file` **runningtime** (`str`, ---)
                 - :dir:`file` **fail** (`str`, ---)
                 - :dir:`file` **abortscan** (`bool`, ---)
                 - :dir:`file` **wipecache** (`bool`, Be aware - this starts a complete library rescan)
             - :dir:`file` **totalgenres** (`num`, ---)
             - :dir:`folder` **totalduration** (`num`, ---)
                 - :dir:`file` **duration_format** (`str`, ---)
             - :dir:`file` **totalartists** (`num`, ---)
             - :dir:`file` **totalalbums** (`num`, ---)
             - :dir:`file` **totalsongs** (`num`, ---)
             - :dir:`file` **totalplaylists** (`num`, ---)
         - :dir:`folder` **server_plugins** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **trackstat** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **get_rating** (`str`, ---)
                 - :dir:`file` **set_rating** (`str`, ---)
         - :dir:`folder` **player_plugins** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **customskip** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **active_filter** (`str`, ---)
                 - :dir:`file` **remove_filter** (`bool`, ---)
         - :dir:`folder` **player** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`file` **playmode** (`str`, ---)
                 - :dir:`file` **playpause** (`bool`, ---)
                 - :dir:`file` **stop** (`bool`, ---)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`folder` **volume_fading** (`num`, ---)
                     - :dir:`file` **goal** (`num`, ---)
                 - :dir:`file` **volume_low** (`num`, ---)
                 - :dir:`file` **volume_high** (`num`, ---)
                 - :dir:`file` **volumeup** (`num`, ---)
                 - :dir:`file` **volumedown** (`num`, ---)
                 - :dir:`file` **set_alarm** (`str`, ---)
                 - :dir:`folder` **alarms** (`dict`, ---)
                     - :dir:`file` **query** (`bool`, ---)
                 - :dir:`file` **sync** (`str`, This can be either a name or MAC address of a connected player. Make sure to include the server struct to fill the lookup table correctly.)
                 - :dir:`file` **sync_status** (`list`, ---)
                 - :dir:`file` **unsync** (`bool`, ---)
                 - :dir:`file` **display** (`str`, ---)
                 - :dir:`file` **connect** (`str`, ip|www.mysqueezebox.com|www.test.mysqueezebox.com)
                 - :dir:`file` **disconnect** (`str`, ip|www.mysqueezebox.com|www.test.mysqueezebox.com)
                 - :dir:`folder` **time** (`num`, ---)
                     - :dir:`file` **poll** (`bool`, ---)
                 - :dir:`file` **forward** (`num`, ---)
                 - :dir:`file` **rewind** (`num`, ---)
                 - :dir:`file` **playitem** (`str`, provide specified song URL, playlist or directory. Gets player right away.)
                 - :dir:`file` **sleep** (`num`, ---)
             - :dir:`folder` **playlist** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **rename_current** (`str`, ---)
                 - :dir:`file` **delete_current** (`bool`, Be careful, instantly deletes the current playlist!)
                 - :dir:`folder` **repeat** (`str`, 0 = Off, 1 = Song, 2 = Playlist)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **shuffle** (`str`, 0 = Off, 1 = Song, 2 = Album)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **index** (`str`, ---)
                 - :dir:`file` **current_name** (`str`, ---)
                 - :dir:`file` **current_url** (`str`, ---)
                 - :dir:`file` **current_id** (`num`, ---)
                 - :dir:`file` **save** (`str`, ---)
                 - :dir:`file` **load** (`str`, ---)
                 - :dir:`file` **loadalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
                 - :dir:`file` **loadtracks** (`str`, loads and plays all songs matching the specified searchparam criteria directly)
                 - :dir:`file` **additem** (`str`, provide specified song URL, playlist or directory)
                 - :dir:`file` **addalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
                 - :dir:`file` **addtracks** (`str`, appends all songs matching the specified searchparam criteria onto the end of the playlist)
                 - :dir:`file` **insertalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
                 - :dir:`file` **insertitem** (`str`, provide specified song URL, playlist or directory)
                 - :dir:`file` **deletesong** (`num`, provide songindex that should be deleted)
                 - :dir:`file` **deletealbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
                 - :dir:`file` **deleteitem** (`str`, provide specified song URL, playlist or directory)
                 - :dir:`file` **tracks** (`num`, ---)
                 - :dir:`file` **clear** (`bool`, Might go berserk, use with care!)
                 - :dir:`file` **preview** (`str`, ---)
                 - :dir:`file` **next** (`num`, ---)
                 - :dir:`file` **previous** (`num`, ---)
                 - :dir:`file` **modified** (`bool`, ---)
             - :dir:`folder` **info** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **player** (`foo`, OrderedDict([('type', 'str'), ('lms_command@instance', 'player.info.player.name'), ('lms_read@instance', True), ('lms_write@instance', True), ('lms_read_group@instance', ['ALL', 'ALL.player', 'ALL.player.info', 'ALL.player.info.player'])]))
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **status_subscribe** (`bool`, ---)
                     - :dir:`file` **status** (`str`, ---)
                     - :dir:`file` **connected** (`bool`, ---)
                     - :dir:`file` **ip** (`str`, ---)
                     - :dir:`file` **modelname** (`str`, ---)
                     - :dir:`file` **firmware** (`str`, ---)
                     - :dir:`file` **name** (`str`, ---)
                     - :dir:`file` **signalstrength** (`num`, ---)
                     - :dir:`file` **albumarturl** (`str`, This item gets automatically defined and overwritten based on 'web_host'/'host' and 'web_port')
                 - :dir:`folder` **currentsong** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **genre** (`str`, ---)
                     - :dir:`file` **artist** (`str`, ---)
                     - :dir:`file` **album** (`str`, ---)
                     - :dir:`file` **title** (`str`, ---)
                     - :dir:`file` **file_path** (`str`, ---)
                     - :dir:`folder` **duration** (`num`, ---)
                         - :dir:`file` **duration_format** (`str`, ---)


database
========


.. treeview::
     - :dir:`folder` **database** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **rescan** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **start** (`str`, "playlists|onlinelibrary|external|full|full file://some/path")
             - :dir:`file` **running** (`bool`, ---)
             - :dir:`folder` **progress** (`str`, ---)
                 - :dir:`file` **poll** (`bool`, ---)
                 - :dir:`file` **starttime** (`str`, ---)
                 - :dir:`file` **info** (`str`, ---)
                 - :dir:`file` **step** (`num`, ---)
                 - :dir:`file` **totalsteps** (`num`, ---)
             - :dir:`file` **runningtime** (`str`, ---)
             - :dir:`file` **fail** (`str`, ---)
             - :dir:`file` **abortscan** (`bool`, ---)
             - :dir:`file` **wipecache** (`bool`, Be aware - this starts a complete library rescan)
         - :dir:`file` **totalgenres** (`num`, ---)
         - :dir:`folder` **totalduration** (`num`, ---)
             - :dir:`file` **duration_format** (`str`, ---)
         - :dir:`file` **totalartists** (`num`, ---)
         - :dir:`file` **totalalbums** (`num`, ---)
         - :dir:`file` **totalsongs** (`num`, ---)
         - :dir:`file` **totalplaylists** (`num`, ---)


player
======


.. treeview::
     - :dir:`folder` **player** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`file` **playmode** (`str`, ---)
             - :dir:`file` **playpause** (`bool`, ---)
             - :dir:`file` **stop** (`bool`, ---)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`folder` **volume_fading** (`num`, ---)
                 - :dir:`file` **goal** (`num`, ---)
             - :dir:`file` **volume_low** (`num`, ---)
             - :dir:`file` **volume_high** (`num`, ---)
             - :dir:`file` **volumeup** (`num`, ---)
             - :dir:`file` **volumedown** (`num`, ---)
             - :dir:`file` **set_alarm** (`str`, ---)
             - :dir:`folder` **alarms** (`dict`, ---)
                 - :dir:`file` **query** (`bool`, ---)
             - :dir:`file` **sync** (`str`, This can be either a name or MAC address of a connected player. Make sure to include the server struct to fill the lookup table correctly.)
             - :dir:`file` **sync_status** (`list`, ---)
             - :dir:`file` **unsync** (`bool`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **connect** (`str`, ip|www.mysqueezebox.com|www.test.mysqueezebox.com)
             - :dir:`file` **disconnect** (`str`, ip|www.mysqueezebox.com|www.test.mysqueezebox.com)
             - :dir:`folder` **time** (`num`, ---)
                 - :dir:`file` **poll** (`bool`, ---)
             - :dir:`file` **forward** (`num`, ---)
             - :dir:`file` **rewind** (`num`, ---)
             - :dir:`file` **playitem** (`str`, provide specified song URL, playlist or directory. Gets player right away.)
             - :dir:`file` **sleep** (`num`, ---)
         - :dir:`folder` **playlist** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **rename_current** (`str`, ---)
             - :dir:`file` **delete_current** (`bool`, Be careful, instantly deletes the current playlist!)
             - :dir:`folder` **repeat** (`str`, 0 = Off, 1 = Song, 2 = Playlist)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **shuffle** (`str`, 0 = Off, 1 = Song, 2 = Album)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **index** (`str`, ---)
             - :dir:`file` **current_name** (`str`, ---)
             - :dir:`file` **current_url** (`str`, ---)
             - :dir:`file` **current_id** (`num`, ---)
             - :dir:`file` **save** (`str`, ---)
             - :dir:`file` **load** (`str`, ---)
             - :dir:`file` **loadalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
             - :dir:`file` **loadtracks** (`str`, loads and plays all songs matching the specified searchparam criteria directly)
             - :dir:`file` **additem** (`str`, provide specified song URL, playlist or directory)
             - :dir:`file` **addalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
             - :dir:`file` **addtracks** (`str`, appends all songs matching the specified searchparam criteria onto the end of the playlist)
             - :dir:`file` **insertalbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
             - :dir:`file` **insertitem** (`str`, provide specified song URL, playlist or directory)
             - :dir:`file` **deletesong** (`num`, provide songindex that should be deleted)
             - :dir:`file` **deletealbum** (`str`, <Genre> <Artist> <Album>. You can use * for any of the entries. Spaces need to be replaced by %20)
             - :dir:`file` **deleteitem** (`str`, provide specified song URL, playlist or directory)
             - :dir:`file` **tracks** (`num`, ---)
             - :dir:`file` **clear** (`bool`, Might go berserk, use with care!)
             - :dir:`file` **preview** (`str`, ---)
             - :dir:`file` **next** (`num`, ---)
             - :dir:`file` **previous** (`num`, ---)
             - :dir:`file` **modified** (`bool`, ---)
         - :dir:`folder` **info** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **player** (`foo`, OrderedDict([('type', 'str'), ('lms_command@instance', 'player.info.player.name'), ('lms_read@instance', True), ('lms_write@instance', True), ('lms_read_group@instance', ['player', 'player.info', 'player.info.player'])]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **status_subscribe** (`bool`, ---)
                 - :dir:`file` **status** (`str`, ---)
                 - :dir:`file` **connected** (`bool`, ---)
                 - :dir:`file` **ip** (`str`, ---)
                 - :dir:`file` **modelname** (`str`, ---)
                 - :dir:`file` **firmware** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`file` **signalstrength** (`num`, ---)
                 - :dir:`file` **albumarturl** (`str`, This item gets automatically defined and overwritten based on 'web_host'/'host' and 'web_port')
             - :dir:`folder` **currentsong** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **genre** (`str`, ---)
                 - :dir:`file` **artist** (`str`, ---)
                 - :dir:`file` **album** (`str`, ---)
                 - :dir:`file` **title** (`str`, ---)
                 - :dir:`file` **file_path** (`str`, ---)
                 - :dir:`folder` **duration** (`num`, ---)
                     - :dir:`file` **duration_format** (`str`, ---)


player_plugins
==============


.. treeview::
     - :dir:`folder` **player_plugins** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **customskip** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **active_filter** (`str`, ---)
             - :dir:`file` **remove_filter** (`bool`, ---)


server
======


.. treeview::
     - :dir:`folder` **server** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`file` **version** (`str`, ---)
         - :dir:`file` **restart** (`bool`, ---)
         - :dir:`file` **listenmode** (`bool`, ---)
         - :dir:`file` **playercount** (`num`, ---)
         - :dir:`file` **favoritescount** (`num`, ---)
         - :dir:`folder` **syncgroups** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **members** (`list`, ---)
             - :dir:`file` **names** (`list`, ---)
         - :dir:`file` **newclient** (`str`, ---)
         - :dir:`file` **forgetclient** (`str`, ---)
         - :dir:`folder` **players** (`dict`, ---)
             - :dir:`folder` **lu_names** (`dict`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **playlists** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **available** (`dict`, ---)
                 - :dir:`folder` **lu_ids** (`dict`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **lu_urls** (`dict`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **rename** (`str`, "needed value:<playlist_id> <newname> with a space inbetween")
             - :dir:`folder` **delete** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)


server_plugins
==============


.. treeview::
     - :dir:`folder` **server_plugins** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **trackstat** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **get_rating** (`str`, ---)
             - :dir:`file` **set_rating** (`str`, ---)


===============
Logik Parameter
===============


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/logic.yaml`` konfiguriert werden:

Keine Logik Parameter in den Metadaten beschrieben - **Bitte in der README nachsehen** (siehe Fußnote)

================
Plugin Functions
================


Das Plugin verfügt über folgende öffentliche Funktionen, die z.B. in Logiken aufgerufen werden können.

**Keine**

.. [#f1] Diese Seite wurde aus den Metadaten des Plugins erzeugt.
