.. |_| unicode:: 0xA0


==============================
Plugin 'pioneer' Konfiguration
==============================


.. index:: Plugins; pioneer Konfiguration

.. image:: /plugins/pioneer/webif/static/img/plugin_logo.svg
   :alt: plugin logo
   :width: 300px
   :height: 300px
   :scale: 50%
   :align: left

.. |br| raw:: html

   <br />

Im folgenden sind etwaige Anforderungen und unterstützte Hardware beschrieben. Danach folgt die               Beschreibung, wie das Plugin **pioneer** konfiguriert wird. Außerdem ist im folgenden               beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann. [#f1]_ 

Es handelt sich bei diesem Plugin um ein **interface Plugin**.

**ACHTUNG**: Dieses Plugin ist als develop gekennzeichnet. Es kann daher sein, dass es                  noch nicht sämtliche Funktionen unterstützt oder noch fehlerhaft ist.



Beschreibung
============




Anforderungen
=============




 - Minimum SmartHomeNG Version: **1.9.5**
 - Minimum Python Version: **3.7**

Konfiguration
=============


Im folgenden ist beschrieben, wie das Plugin **pioneer** konfiguriert wird. Außerdem ist im folgenden beschrieben, wie das Plugin in den Item Definitionen genutzt werden kann.


=========
Parameter
=========


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/plugin.yaml`` konfiguriert werden:


autoconnect
===========




 - Datentyp: **bool**


autoreconnect
=============




 - Datentyp: **bool**
 - Standardwert: **True**


baudrate
========




 - Datentyp: **num**
 - Standardwert: **9600**


binary
======




 - Datentyp: **bool**
 - Standardwert: **False**


bytesize
========




 - Datentyp: **num**
 - Standardwert: **8**


conn_type
=========




 - Datentyp: **str**

 - Mögliche Werte:

   - 
   - **net_tcp_client**
   - **serial_async**


connect_cycle
=============




 - Datentyp: **num**
 - Standardwert: **3**


connect_retries
===============




 - Datentyp: **num**
 - Standardwert: **5**


delay_initial_read
==================




 - Datentyp: **num**
 - Standardwert: **0**


host
====




 - Datentyp: **str**


model
=====




 - Datentyp: **str**

 - Mögliche Werte:

   - 
   - **SC-LX87**
   - **SC-LX77**
   - **SC-LX57**
   - **SC-2023**
   - **SC-1223**
   - **VSX-1123**
   - **VSX-923**


parity
======




 - Datentyp: **str**
 - Standardwert: **N**

 - Mögliche Werte:

   - **N**
   - **E**
   - **O**
   - **M**
   - **S**


port
====




 - Datentyp: **int**
 - Standardwert: **8102**


resume_initial_read
===================




 - Datentyp: **bool**


retry_cycle
===========




 - Datentyp: **num**
 - Standardwert: **30**


retry_suspend
=============




 - Datentyp: **num**
 - Standardwert: **3**


send_retries
============




 - Datentyp: **num**
 - Standardwert: **0**


send_retries_cycle
==================




 - Datentyp: **num**
 - Standardwert: **1**

 - Minimalwert: **1**

serialport
==========




 - Datentyp: **str**


stopbits
========




 - Datentyp: **num**
 - Standardwert: **1**


suspend_item
============




 - Datentyp: **str**


terminator
==========




 - Datentyp: **str**
 - Standardwert: **\r**


timeout
=======




 - Datentyp: **num**
 - Standardwert: **3**


==============
Item Attribute
==============


Das Plugin unterstützt folgende Item Attribute, die in den Dateien im Verzeichnis  ``../items`` verwendet werden:


pioneer_command
===============




 - Datentyp: **str**


pioneer_lookup
==============




 - Datentyp: **str**


pioneer_read
============




 - Datentyp: **bool**


pioneer_read_cycle
==================




 - Datentyp: **num**


pioneer_read_group
==================




 - Datentyp: **list(str)**


pioneer_read_group_trigger
==========================




 - Datentyp: **str**


pioneer_read_initial
====================




 - Datentyp: **bool**


pioneer_write
=============




 - Datentyp: **bool**


============
Item-Structs
============


Das Plugin stellt die folgenden Item-Structs zur Verfügung. Diese Informationen sind aus der `plugin.yaml` entnommen und möglicherweise nicht vollständig.


ALL
===


.. treeview::
     - :dir:`folder` **ALL** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['ALL', 'ALL.general', 'ALL.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


SC-1223
=======


.. treeview::
     - :dir:`folder` **SC-1223** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['SC-1223', 'SC-1223.general', 'SC-1223.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **bass** (`num`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


SC-2023
=======


.. treeview::
     - :dir:`folder` **SC-2023** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['SC-2023', 'SC-2023.general', 'SC-2023.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **surroundposition** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xover** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **bass** (`num`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, For some receivers it might be necessary to change speakersystem to Zone 2 and hdzone for HDMI on.)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


SC-LX57
=======


.. treeview::
     - :dir:`folder` **SC-LX57** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`folder` **amp** (`str`, 0 = AMP, 1 = THR)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['SC-LX57', 'SC-LX57.general', 'SC-LX57.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **surroundposition** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xover** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **loudness** (`bool`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **bass** (`num`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, For some receivers it might be necessary to change speakersystem to Zone 2 and hdzone for HDMI on.)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


SC-LX77
=======


.. treeview::
     - :dir:`folder` **SC-LX77** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`folder` **amp** (`str`, 0 = AMP, 1 = THR)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['SC-LX77', 'SC-LX77.general', 'SC-LX77.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **surroundposition** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xover** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **loudness** (`bool`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **bass** (`num`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, For some receivers it might be necessary to change speakersystem to Zone 2 and hdzone for HDMI on.)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


SC-LX87
=======


.. treeview::
     - :dir:`folder` **SC-LX87** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`folder` **amp** (`str`, 0 = AMP, 1 = THR)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['SC-LX87', 'SC-LX87.general', 'SC-LX87.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **surroundposition** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xover** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **loudness** (`bool`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **bass** (`num`, ---)
         - :dir:`folder` **zone3** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, For some receivers it might be necessary to change speakersystem to Zone 2 and hdzone for HDMI on.)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


VSX-1123
========


.. treeview::
     - :dir:`folder` **VSX-1123** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['VSX-1123', 'VSX-1123.general', 'VSX-1123.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


VSX-923
=======


.. treeview::
     - :dir:`folder` **VSX-923** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **general** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **error** (`str`, ---)
             - :dir:`file` **display** (`str`, ---)
             - :dir:`file` **pqls** (`bool`, ---)
             - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
             - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
             - :dir:`file` **multizone** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['VSX-923', 'VSX-923.general', 'VSX-923.general.settings']), ('pioneer_read_initial@instance', True)]))
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **language** (`str`, ---)
                 - :dir:`file` **name** (`str`, ---)
                 - :dir:`folder` **speakersystem** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **xcurve** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`folder` **hdmi** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **hdzone** (`bool`, ---)
                     - :dir:`file` **control** (`bool`, ---)
                     - :dir:`file` **controlmode** (`bool`, ---)
                     - :dir:`file` **arc** (`bool`, ---)
                     - :dir:`folder` **standbythrough** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **tuner** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **tunerpreset** (`str`, ---)
             - :dir:`file` **tunerpresetup** (`bool`, ---)
             - :dir:`file` **tunerpresetdown** (`bool`, ---)
             - :dir:`file` **title** (`str`, ---)
             - :dir:`file` **genre** (`str`, ---)
             - :dir:`file` **station** (`str`, ---)
         - :dir:`folder` **zone1** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
                 - :dir:`folder` **sound** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **channel_level** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **front_left** (`num`, ---)
                         - :dir:`file` **front_right** (`num`, ---)
                         - :dir:`file` **front_center** (`num`, ---)
                         - :dir:`file` **surround_left** (`num`, ---)
                         - :dir:`file` **surround_right** (`num`, ---)
                         - :dir:`file` **front_height_left** (`num`, ---)
                         - :dir:`file` **front_height_right** (`num`, ---)
                         - :dir:`file` **front_wide_left** (`num`, ---)
                         - :dir:`file` **front_wide_right** (`num`, ---)
                         - :dir:`file` **subwoofer** (`num`, ---)
                     - :dir:`folder` **tone_control** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`file` **tone** (`bool`, ---)
                         - :dir:`file` **treble** (`num`, ---)
                         - :dir:`file` **trebleup** (`bool`, ---)
                         - :dir:`file` **trebledown** (`bool`, ---)
                         - :dir:`file` **bass** (`num`, ---)
                         - :dir:`file` **bassup** (`bool`, ---)
                         - :dir:`file` **bassdown** (`bool`, ---)
                     - :dir:`folder` **general** (`foo`, ---)
                         - :dir:`file` **read** (`bool`, ---)
                         - :dir:`folder` **hdmiout** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`folder` **hdmiaudio** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **dialog** (`num`, ---)
                         - :dir:`file` **dialogup** (`bool`, ---)
                         - :dir:`file` **dialogdown** (`bool`, ---)
                         - :dir:`folder` **listeningmode** (`str`, ---)
                             - :dir:`file` **lookup** (`list`, ---)
                         - :dir:`file` **playingmode** (`str`, ---)
                         - :dir:`folder` **speakers** (`num`, ---)
                             - :dir:`file` **speakera** (`bool`, ---)
                             - :dir:`file` **speakerb** (`bool`, ---)
         - :dir:`folder` **zone2** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **power** (`bool`, ---)
                     - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
                 - :dir:`file` **mute** (`bool`, ---)
                 - :dir:`file` **volume** (`num`, ---)
                 - :dir:`file` **volumeup** (`bool`, ---)
                 - :dir:`file` **volumedown** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
                 - :dir:`file` **inputup** (`bool`, ---)
                 - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **hdzone** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **control** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **power** (`bool`, ---)
                 - :dir:`folder` **input** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **settings** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


general
=======


.. treeview::
     - :dir:`folder` **general** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`file` **error** (`str`, ---)
         - :dir:`file` **display** (`str`, ---)
         - :dir:`file` **pqls** (`bool`, ---)
         - :dir:`file` **dimmer** (`num`, 0 = very bright, 1 = bright, 2 = dark, 3 = off)
         - :dir:`file` **sleep** (`num`, 0 = off, 30 = 30 minutes, 60 = 60 minutes, 90 = 90 minutes)
         - :dir:`folder` **amp** (`str`, 0 = AMP, 1 = THR)
             - :dir:`file` **lookup** (`list`, ---)
         - :dir:`file` **multizone** (`bool`, ---)
         - :dir:`folder` **settings** (`foo`, OrderedDict([('type', 'str'), ('pioneer_command@instance', 'general.settings.name'), ('pioneer_read@instance', True), ('pioneer_write@instance', True), ('pioneer_read_group@instance', ['general', 'general.settings']), ('pioneer_read_initial@instance', True)]))
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **language** (`str`, ---)
             - :dir:`file` **name** (`str`, ---)
             - :dir:`folder` **speakersystem** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **surroundposition** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **xover** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`folder` **xcurve** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **loudness** (`bool`, ---)
             - :dir:`file` **initialvolume** (`num`, ---)
             - :dir:`file` **mutelevel** (`num`, ---)
             - :dir:`folder` **hdmi** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`file` **hdzone** (`bool`, ---)
                 - :dir:`file` **control** (`bool`, ---)
                 - :dir:`file` **controlmode** (`bool`, ---)
                 - :dir:`file` **arc** (`bool`, ---)
                 - :dir:`folder` **standbythrough** (`str`, ---)
                     - :dir:`file` **lookup** (`list`, ---)


hdzone
======


.. treeview::
     - :dir:`folder` **hdzone** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **power** (`bool`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)


tuner
=====


.. treeview::
     - :dir:`folder` **tuner** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`file` **tunerpreset** (`str`, ---)
         - :dir:`file` **tunerpresetup** (`bool`, ---)
         - :dir:`file` **tunerpresetdown** (`bool`, ---)
         - :dir:`file` **title** (`str`, ---)
         - :dir:`file` **genre** (`str`, ---)
         - :dir:`file` **station** (`str`, ---)


zone1
=====


.. treeview::
     - :dir:`folder` **zone1** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **power** (`bool`, ---)
                 - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputup** (`bool`, ---)
             - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **standby** (`num`, 0 = OFF, 15 = 15 minutes, 30 = 30 minutes, 60 = 60 minutes)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)
                     - :dir:`file` **front_center** (`num`, ---)
                     - :dir:`file` **surround_left** (`num`, ---)
                     - :dir:`file` **surround_right** (`num`, ---)
                     - :dir:`file` **front_height_left** (`num`, ---)
                     - :dir:`file` **front_height_right** (`num`, ---)
                     - :dir:`file` **front_wide_left** (`num`, ---)
                     - :dir:`file` **front_wide_right** (`num`, ---)
                     - :dir:`file` **subwoofer** (`num`, ---)
                 - :dir:`folder` **tone_control** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **tone** (`bool`, ---)
                     - :dir:`file` **treble** (`num`, ---)
                     - :dir:`file` **trebleup** (`bool`, ---)
                     - :dir:`file` **trebledown** (`bool`, ---)
                     - :dir:`file` **bass** (`num`, ---)
                     - :dir:`file` **bassup** (`bool`, ---)
                     - :dir:`file` **bassdown** (`bool`, ---)
                 - :dir:`folder` **general** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`folder` **hdmiout** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`folder` **hdmiaudio** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **dialog** (`num`, ---)
                     - :dir:`file` **dialogup** (`bool`, ---)
                     - :dir:`file` **dialogdown** (`bool`, ---)
                     - :dir:`folder` **listeningmode** (`str`, ---)
                         - :dir:`file` **lookup** (`list`, ---)
                     - :dir:`file` **playingmode** (`str`, ---)
                     - :dir:`folder` **speakers** (`num`, ---)
                         - :dir:`file` **speakera** (`bool`, ---)
                         - :dir:`file` **speakerb** (`bool`, ---)


zone2
=====


.. treeview::
     - :dir:`folder` **zone2** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **power** (`bool`, ---)
                 - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputup** (`bool`, ---)
             - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)
                 - :dir:`folder` **tone_control** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **tone** (`bool`, ---)
                     - :dir:`file` **treble** (`num`, ---)
                     - :dir:`file` **bass** (`num`, ---)


zone3
=====


.. treeview::
     - :dir:`folder` **zone3** (`foo`, ---)
         - :dir:`file` **read** (`bool`, ---)
         - :dir:`folder` **control** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`folder` **power** (`bool`, For some receivers it might be necessary to change speakersystem to Zone 2 and hdzone for HDMI on.)
                 - :dir:`file` **readdelay** (`num`, After turning on a zone, the most likely needs some time to react to read commands. If not, set this value to 0)
             - :dir:`file` **mute** (`bool`, ---)
             - :dir:`file` **volume** (`num`, ---)
             - :dir:`file` **volumeup** (`bool`, ---)
             - :dir:`file` **volumedown** (`bool`, ---)
             - :dir:`folder` **input** (`str`, ---)
                 - :dir:`file` **lookup** (`list`, ---)
             - :dir:`file` **inputup** (`bool`, ---)
             - :dir:`file` **inputdown** (`bool`, ---)
         - :dir:`folder` **settings** (`foo`, ---)
             - :dir:`file` **read** (`bool`, ---)
             - :dir:`file` **standby** (`num`, 0 = OFF, 0.5 = 30 minutes, 1 = 1 hour, 3 = 3 hours, 6 = 6 hours, 9 = 9 hours)
             - :dir:`folder` **sound** (`foo`, ---)
                 - :dir:`file` **read** (`bool`, ---)
                 - :dir:`folder` **channel_level** (`foo`, ---)
                     - :dir:`file` **read** (`bool`, ---)
                     - :dir:`file` **front_left** (`num`, ---)
                     - :dir:`file` **front_right** (`num`, ---)


===============
Logik Parameter
===============


Das Plugin verfügt über folgende Parameter, die in der Datei ``../etc/logic.yaml`` konfiguriert werden:

Keine Logik Parameter in den Metadaten beschrieben - **Bitte in der README nachsehen** (siehe Fußnote)

================
Plugin Functions
================


Das Plugin verfügt über folgende öffentliche Funktionen, die z.B. in Logiken aufgerufen werden können.

**Keine**

.. [#f1] Diese Seite wurde aus den Metadaten des Plugins erzeugt.
